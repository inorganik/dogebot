var enableLogging=true,itemIncrement=10,itemThreshold=itemIncrement,itemThresholdLimit,rigThresholdLimit,buyLoopIntervalTime=1e3,mineLoopIntervalTime=100,buyInterval,mineInterval,miner=document.getElementById("miner"),counts={bonusCoins:0,flyingCoins:0},lastBonusCoinClick=0,lastFlyingCoinClick=0,locations=["EARTH","MOON","MARS","????(4)","????(5)","????(6)"],locationIndex=0,waitingForIndex=-1;function getLocation(){var loc=document.getElementById("location");return loc.children[0].textContent}function getNextLocation(){if(locations[locationIndex+1]){return locations[locationIndex+1]}else{return""}}locationIndex=locations.indexOf(getLocation());function getTimePlayed(){return document.getElementById("timeplayed").textContent}function mapActionToCount(action){if(/shibe/.test(action))return"shibes";if(/kennel/.test(action))return"kennels";if(/kitten/.test(action))return"kittens";if(/rocket/.test(action))return"rockets";if(/base/.test(action))return"bases";if(/rig/.test(action))return"rigs"}function parseCount(what){var whats=document.getElementById(what);if(whats){if(whats.textContent.replace(/\D+/g,"").length){return parseInt(whats.textContent.replace(/\D+/g,""))}else{return 0}}else{return 0}}function getCount(what){var amount=parseCount(what);counts[what]=amount;return amount}function resetCounts(){for(var key in counts){counts[key]=0}}function simulateClick(el){if(el){$(el).trigger("mousedown");setTimeout(function(){$(el).trigger("mouseup")},10)}}function autoClick(what){var btn=document.getElementById(what);if(btn&&!btn.getAttribute("disabled")){$(btn).trigger("click")}}function stopLoops(){clearInterval(buyInterval);clearInterval(mineInterval)}function setThresholdLimit(){var loc=getLocation();itemThresholdLimit=itemIncrement*itemIncrement;switch(loc){case"EARTH":case"MOON":rigThresholdLimit=itemIncrement;break;case"MARS":rigThresholdLimit=Math.pow(itemIncrement,2);break;default:rigThresholdLimit=Math.pow(itemIncrement,3);break}}function resetLoops(){stopLoops();resetCounts();setThresholdLimit();if(enableLogging)console.warn("[Dogebot] started! hold on to your butts 🚬",getTimePlayed());mineInterval=setInterval(function(){simulateClick(miner);var bonuscoin=document.getElementById("bonuscoin"),flyingcoin=document.getElementById("flyingcoin"),time;if(bonuscoin){time=(new Date).getTime();if(time-lastBonusCoinClick>6e3){simulateClick(bonuscoin);counts.bonusCoins++;if(enableLogging)console.warn("[Dogebot] clicked a bonus coin! 🤑",getTimePlayed());lastBonusCoinClick=time}}if(flyingcoin){time=(new Date).getTime();if(time-lastFlyingCoinClick>1400){simulateClick(flyingcoin);counts.flyingCoins++;if(enableLogging)console.warn("[Dogebot] clicked a flying coin! 💸",getTimePlayed());lastFlyingCoinClick=time}}},mineLoopIntervalTime);buyInterval=setInterval(function(){var thresholdMet=false,rigs=getCount("rigs"),loc=getLocation(),thresholdIteration=itemThreshold/itemIncrement,maxThresholdLevelReached=thresholdIteration===itemIncrement;autoClick("upgradeextras");autoClick("upgradeclicks");if(loc===getNextLocation()){locationIndex++;itemThreshold=itemIncrement;resetCounts();setThresholdLimit();if(enableLogging)console.warn("[Dogebot] YOU REACHED "+locations[locationIndex]+"!!!",getTimePlayed())}else if(rigs>=rigThresholdLimit){if(locationIndex!==waitingForIndex){var d=new Date;if(enableLogging)console.warn("[Dogebot] stopped buying to get ready for "+getNextLocation()+" launch 🚀",getTimePlayed());waitingForIndex=locationIndex}var launchbutton=document.getElementById("launchbutton");if(launchbutton&&$(launchbutton).css("display")!=="none"){$(launchbutton).trigger("click");if(enableLogging)console.warn("[Dogebot] blastoff!!!!! 🚀",getTimePlayed())}return}var rigThreshold=maxThresholdLevelReached?rigThresholdLimit:thresholdIteration;if(rigs<rigThreshold){autoClick("buyrig")}else{autoClick("upgraderigs");thresholdMet=true}var bases=getCount("bases");var baseThreshold=loc==="EARTH"?itemThresholdLimit/2*thresholdIteration:itemThresholdLimit;if(bases<baseThreshold){autoClick("buybase")}else{autoClick("upgradebases")}var rockets=getCount("rockets");if(rockets<itemThreshold&&rockets<itemThresholdLimit){autoClick("buyrocket")}else{autoClick("upgraderockets")}var kittens=getCount("kittens");if(kittens<itemThreshold&&kittens<itemThresholdLimit){autoClick("buykitten")}else{autoClick("upgradekittens")}var kennels=getCount("kennels");if(kennels<itemThreshold+itemIncrement&&kennels<itemThresholdLimit){autoClick("buykennel")}else{autoClick("upgradekennels")}var shibes=getCount("shibes");if(shibes<itemThreshold+itemIncrement&&shibes<itemThresholdLimit){autoClick("buyshibe")}else{autoClick("upgradeshibes")}if(thresholdMet){itemThreshold+=itemIncrement;if(enableLogging)console.warn("[Dogebot] upped the threshhold level! 🆙",thresholdIteration,getTimePlayed())}},buyLoopIntervalTime)}resetLoops();