var enableLogging=true,itemIncrement=10,itemThreshold=itemIncrement,buyLoopIntervalTime=1e3,mineLoopIntervalTime=100,miner=document.getElementById("miner"),itemThresholdLimit,thresholdIteration,rigThresholdLimit,rigThreshold,baseThreshold,maxThresholdLevelReached,buyInterval,mineInterval,pollInterval,coinsLastHour=[],coinsPerSecLastHour=[],increasesInCoinsPerSec=[],lastPerSec=0,counts={},lastBonusCoinClick=0,lastFlyingCoinClick=0,locations=["EARTH","MOON","MARS","????(4)","????(5)","????(6)"],locationIndex=0,waitingForIndex=-1,loggedFinalThreshold=false;function getLocation(){var loc=document.getElementById("location");return loc.children[0].textContent}function getNextLocation(){if(locations[locationIndex+1]){return locations[locationIndex+1]}else{return""}}locationIndex=locations.indexOf(getLocation());function getTimePlayed(){return document.getElementById("timeplayed").textContent}function mapActionToCount(action){if(/shibe/.test(action))return"shibes";if(/kennel/.test(action))return"kennels";if(/kitten/.test(action))return"kittens";if(/rocket/.test(action))return"rockets";if(/base/.test(action))return"bases";if(/rig/.test(action))return"rigs"}function parseCount(what){var whats=document.getElementById(what);if(whats){if(whats.textContent.replace(/\D+/g,"").length){return parseInt(whats.textContent.replace(/\D+/g,""))}else{return 0}}else{return 0}}function getCount(what){var amount=parseCount(what);counts[what]=amount;return amount}function getCoinCount(what){var whats=document.getElementById(what);if(whats&&whats.children[0]&&whats.children[0].textContent.replace(/\D+/g,"").length){return parseInt(whats.textContent.replace(/\D+/g,""))}else{return 0}}function resetCounts(){for(var key in counts){counts[key]=0}loggedFinalThreshold=false}function simulateClick(el){if(el){$(el).trigger("mousedown");setTimeout(function(){$(el).trigger("mouseup")},10)}}function autoClick(what){var btn=document.getElementById(what);if(btn&&!btn.getAttribute("disabled")){$(btn).trigger("click")}}function stopLoops(){clearInterval(buyInterval);clearInterval(mineInterval);clearInterval(pollInterval)}function setThresholdLimit(){var loc=getLocation();itemThresholdLimit=itemIncrement*itemIncrement;switch(loc){case"EARTH":rigThresholdLimit=itemIncrement;baseThreshold=itemThresholdLimit/2*thresholdIteration;break;case"MOON":rigThresholdLimit=itemIncrement;baseThreshold=itemThresholdLimit/2;break;case"MARS":rigThresholdLimit=Math.pow(itemIncrement,3)/2;baseThreshold=itemThresholdLimit;break;default:rigThresholdLimit=Math.pow(itemIncrement,3);baseThreshold=itemThresholdLimit;break}}function resetLoops(){stopLoops();resetCounts();setThresholdLimit();if(enableLogging)console.warn("[Dogebot] started! hold on to your butts 🚬",getTimePlayed());mineInterval=setInterval(function(){simulateClick(miner);var bonuscoin=document.getElementById("bonuscoin"),flyingcoin=document.getElementById("flyingcoin"),time;if(bonuscoin){time=(new Date).getTime();if(time-lastBonusCoinClick>6e3){simulateClick(bonuscoin);if(enableLogging)console.warn("[Dogebot] clicked a bonus coin! 🤑",getTimePlayed());lastBonusCoinClick=time}}if(flyingcoin){time=(new Date).getTime();if(time-lastFlyingCoinClick>1e3){simulateClick(flyingcoin);if(enableLogging)console.warn("[Dogebot] clicked a flying coin! 💸",getTimePlayed());lastFlyingCoinClick=time}}},mineLoopIntervalTime);buyInterval=setInterval(function(){var thresholdMet=false,rigs=getCount("rigs"),loc=getLocation();thresholdIteration=itemThreshold/itemIncrement;maxThresholdLevelReached=thresholdIteration===itemIncrement;autoClick("upgradeextras");autoClick("upgradeclicks");if(rigs>0){autoClick("upgraderigs")}if(loc===getNextLocation()){locationIndex++;itemThreshold=itemIncrement;resetCounts();setThresholdLimit();if(enableLogging)console.warn("[Dogebot] YOU REACHED "+locations[locationIndex]+"!!!",getTimePlayed())}else if(rigs>=rigThresholdLimit){if(locationIndex!==waitingForIndex){var d=new Date;if(enableLogging)console.warn("[Dogebot] stopped buying to get ready for "+getNextLocation()+" launch 🚀",getTimePlayed());waitingForIndex=locationIndex}var launchbutton=document.getElementById("launchbutton");if(launchbutton&&$(launchbutton).css("display")!=="none"){$(launchbutton).trigger("click");if(enableLogging)console.warn("[Dogebot] blastoff!!!!! 🚀",getTimePlayed())}return}if(maxThresholdLevelReached){rigThreshold=rigThresholdLimit;if(!loggedFinalThreshold){if(enableLogging)console.warn("[Dogebot] upped the threshold to the highest level! 🆙",thresholdIteration,getTimePlayed());loggedFinalThreshold=true}}else{rigThreshold=thresholdIteration;if(rigs>=thresholdIteration)thresholdMet=true}if(rigs<rigThreshold){autoClick("buyrig")}var bases=getCount("bases");if(bases<baseThreshold){autoClick("buybase")}else{autoClick("upgradebases")}var rockets=getCount("rockets");if(rockets<itemThreshold&&rockets<itemThresholdLimit){autoClick("buyrocket")}else{autoClick("upgraderockets")}var kittens=getCount("kittens");if(kittens<itemThreshold&&kittens<itemThresholdLimit){autoClick("buykitten")}else{autoClick("upgradekittens")}var kennels=getCount("kennels");if(kennels<itemThreshold+itemIncrement&&kennels<itemThresholdLimit){autoClick("buykennel")}else{autoClick("upgradekennels")}var shibes=getCount("shibes");if(shibes<itemThreshold+itemIncrement&&shibes<itemThresholdLimit){autoClick("buyshibe")}else{autoClick("upgradeshibes")}if(thresholdMet&&!maxThresholdLevelReached){itemThreshold+=itemIncrement;if(enableLogging)console.warn("[Dogebot] upped the threshold level! 🆙",thresholdIteration,getTimePlayed())}},buyLoopIntervalTime);setTimeout(function(){pollInterval=setInterval(function(){var coins=getCoinCount("mined");var persec=getCoinCount("persec");var lastMinuteIncrease=Math.max(0,persec-lastPerSec);coinsLastHour.push(coins);increasesInCoinsPerSec.push(lastMinuteIncrease);coinsPerSecLastHour.push(persec);if(coinsLastHour.length>60)coinsLastHour.shift();if(coinsPerSecLastHour.length>60)coinsPerSecLastHour.shift();if(increasesInCoinsPerSec.length>60)increasesInCoinsPerSec.shift();lastPerSec=persec;if(pollInterval%60===0&&enableLogging&&console.chart!==undefined){console.warn("[Dogebot] increase in dogecoins per second over the last hour: 📈",getTimePlayed());console.chart(increasesInCoinsPerSec,{color:"gray"})}},3e4)},500)}resetLoops();